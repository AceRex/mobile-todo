import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const create = () => {
  return (
    <SafeAreaView>
      <Text>create</Text>
    </SafeAreaView>
  );
};

export default create;
